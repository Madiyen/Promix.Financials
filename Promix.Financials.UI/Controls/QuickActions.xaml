<UserControl
    x:Class="Promix.Financials.UI.Controls.QuickActions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="SaaSActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter x:Name="ContentPresenter"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          Padding="{TemplateBinding Padding}"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="12" Padding="24" Translation="0,2,0">
        <Border.Shadow>
            <ThemeShadow />
        </Border.Shadow>
        <StackPanel Spacing="24">
            <StackPanel Spacing="4">
                <TextBlock Text="إجراءات سريعة" FontSize="18" FontWeight="SemiBold" Foreground="#0F172A"/>
                <TextBlock Text="اختصارات تشغيلية مباشرة." FontSize="13" Foreground="#64748B"/>
            </StackPanel>

            <Grid ColumnSpacing="12" RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Receipt -->
                <Button Grid.Row="0" Grid.Column="0" Click="ReceiptVoucher_Click" Style="{StaticResource SaaSActionButtonStyle}">
                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Width="32" Height="32" Background="#DCFCE7" CornerRadius="8">
                                <FontIcon Glyph="&#xE8C7;" Foreground="#166534" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="سند قبض" FontSize="13" FontWeight="Medium" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>

                <!-- Payment -->
                <Button Grid.Row="0" Grid.Column="1" Click="PaymentVoucher_Click" Style="{StaticResource SaaSActionButtonStyle}">
                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Width="32" Height="32" Background="#FEE2E2" CornerRadius="8">
                                <FontIcon Glyph="&#xEAFD;" Foreground="#991B1B" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="سند صرف" FontSize="13" FontWeight="Medium" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>

                <!-- Transfer -->
                <Button Grid.Row="1" Grid.Column="0" Click="TransferVoucher_Click" Style="{StaticResource SaaSActionButtonStyle}">
                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Width="32" Height="32" Background="#E0E7FF" CornerRadius="8">
                                <FontIcon Glyph="&#xE8AB;" Foreground="#3730A3" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="تحويل" FontSize="13" FontWeight="Medium" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>

                <!-- Journal -->
                <Button Grid.Row="1" Grid.Column="1" Click="DailyJournal_Click" Style="{StaticResource SaaSActionButtonStyle}">
                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Width="32" Height="32" Background="#F3E8FF" CornerRadius="8">
                                <FontIcon Glyph="&#xE8A5;" Foreground="#6B21A8" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="قيد يومية" FontSize="13" FontWeight="Medium" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>

                <!-- Accounts -->
                <Button Grid.Row="2" Grid.Column="0" Click="Accounts_Click" Style="{StaticResource SaaSActionButtonStyle}">
                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Width="32" Height="32" Background="#FEF3C7" CornerRadius="8">
                                <FontIcon Glyph="&#xE8D4;" Foreground="#92400E" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="الحسابات" FontSize="13" FontWeight="Medium" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>

                <!-- Reports -->
                <Button Grid.Row="2" Grid.Column="1" Click="Reports_Click" Style="{StaticResource SaaSActionButtonStyle}">
                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Width="32" Height="32" Background="#CFFAFE" CornerRadius="8">
                                <FontIcon Glyph="&#xE9D2;" Foreground="#155E75" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="التقارير" FontSize="13" FontWeight="Medium" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Button>
            </Grid>
            
            <Border Background="#F1F5F9" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="تحتاج مساعدة؟" FontSize="12" FontWeight="SemiBold" Foreground="#0F172A"/>
                    <TextBlock Text="يمكنك دائماً الرجوع لمركز المساعدة أو التواصل مع الدعم الفني." FontSize="11" Foreground="#64748B" TextWrapping="WrapWholeWords"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>