<UserControl
    x:Class="Promix.Financials.UI.Controls.QuickActions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="SaaSActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter x:Name="ContentPresenter"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          Padding="{TemplateBinding Padding}"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource BorderBrushStrong}"
            BorderThickness="1"
            CornerRadius="16"
            Padding="20">
        <StackPanel Spacing="18">
            <Grid>
                <StackPanel Spacing="4">
                    <TextBlock Text="لوحة الأوامر السريعة" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                    <TextBlock Text="أكثر الإجراءات استخدامًا أثناء العمل اليومي." FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
                </StackPanel>
            </Grid>

            <Border Background="{StaticResource CardSubtleBackgroundBrush}"
                    CornerRadius="14"
                    Padding="8">
                <Grid ColumnSpacing="8" RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Row="0" Grid.Column="0" Click="ReceiptVoucher_Click" Style="{StaticResource SaaSActionButtonStyle}">
                        <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrushStrong}" BorderThickness="1" CornerRadius="12" Padding="12,10">
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" Background="#DCFCE7" CornerRadius="10">
                                    <FontIcon Glyph="&#xE8C7;" Foreground="#166534" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="سند قبض" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                <FontIcon Grid.Column="2" Glyph="&#xE72A;" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Button>

                    <Button Grid.Row="0" Grid.Column="1" Click="PaymentVoucher_Click" Style="{StaticResource SaaSActionButtonStyle}">
                        <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrushStrong}" BorderThickness="1" CornerRadius="12" Padding="12,10">
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" Background="#FEE2E2" CornerRadius="10">
                                    <FontIcon Glyph="&#xEAFD;" Foreground="#991B1B" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="سند صرف" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                <FontIcon Grid.Column="2" Glyph="&#xE72A;" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Button>

                    <Button Grid.Row="1" Grid.Column="0" Click="TransferVoucher_Click" Style="{StaticResource SaaSActionButtonStyle}">
                        <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrushStrong}" BorderThickness="1" CornerRadius="12" Padding="12,10">
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" Background="#DBEAFE" CornerRadius="10">
                                    <FontIcon Glyph="&#xE8AB;" Foreground="#1D4ED8" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="تحويل" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                <FontIcon Grid.Column="2" Glyph="&#xE72A;" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Button>

                    <Button Grid.Row="1" Grid.Column="1" Click="DailyJournal_Click" Style="{StaticResource SaaSActionButtonStyle}">
                        <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrushStrong}" BorderThickness="1" CornerRadius="12" Padding="12,10">
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" Background="#F3E8FF" CornerRadius="10">
                                    <FontIcon Glyph="&#xE8A5;" Foreground="#6B21A8" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="قيد يومية" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                <FontIcon Grid.Column="2" Glyph="&#xE72A;" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Button>

                    <Button Grid.Row="2" Grid.Column="0" Click="Accounts_Click" Style="{StaticResource SaaSActionButtonStyle}">
                        <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrushStrong}" BorderThickness="1" CornerRadius="12" Padding="12,10">
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" Background="#FEF3C7" CornerRadius="10">
                                    <FontIcon Glyph="&#xE8D4;" Foreground="#92400E" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="الحسابات" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                <FontIcon Grid.Column="2" Glyph="&#xE72A;" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Button>

                    <Button Grid.Row="2" Grid.Column="1" Click="Reports_Click" Style="{StaticResource SaaSActionButtonStyle}">
                        <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrushStrong}" BorderThickness="1" CornerRadius="12" Padding="12,10">
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" Background="#CFFAFE" CornerRadius="10">
                                    <FontIcon Glyph="&#xE9D2;" Foreground="#155E75" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="التقارير" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                                <FontIcon Grid.Column="2" Glyph="&#xE72A;" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Button>
                </Grid>
            </Border>

            <Border Background="{StaticResource CardSubtleBackgroundBrush}"
                    CornerRadius="12"
                    Padding="12,10">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xE946;" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1"
                               Text="نفّذ أوامرك اليومية من هنا بدون مغادرة لوحة التحكم."
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               TextWrapping="WrapWholeWords"/>
                </Grid>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
