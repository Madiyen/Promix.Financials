<Page
    x:Class="Promix.Financials.UI.Views.CompanySelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Spacing="6">
            <TextBlock Text="اختر الشركة"
                       FontSize="22"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Right"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

            <TextBlock x:Name="ErrorText"
                       Text=""
                       Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Right"/>
        </StackPanel>

        <!-- List -->
        <ListView x:Name="CompaniesList"
                  Grid.Row="1"
                  Margin="0,16,0,16"
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  ItemClick="CompaniesList_ItemClick"
                  SelectionChanged="CompaniesList_SelectionChanged">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="{Binding DisplayName}"
           FontSize="16"
           FontWeight="SemiBold"
           Foreground="{ThemeResource TextFillColorPrimaryBrush}"
           HorizontalAlignment="Right"/>

                        <Grid Grid.Row="1" Margin="0,4,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{Binding BaseCurrency}"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       HorizontalAlignment="Right"/>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding AccountingStartDateText}"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       HorizontalAlignment="Left"
                                       Language="en-US"/>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Left">
            <Button Content="شركة جديدة"
            Click="OnNewCompanyClicked"/>

            <Button x:Name="SelectButton"
            Content="اختيار"
            Click="OnSelectClicked"
            IsEnabled="False"/>

            <Button x:Name="ResetDataButton"
            Content="إعادة تهيئة البيانات"
            Click="OnResetDataClicked"
            Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Page>
