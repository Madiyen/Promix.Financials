<Window
    x:Class="Promix.Financials.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Promix.Financials.UI.Controls">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="280" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: Header + Content -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <controls:AppHeader x:Name="Header"
                                Grid.Row="0"/>

            <InfoBar x:Name="GlobalInfoBar"
                     Grid.Row="1"
                     Margin="24,10,24,0"
                     IsOpen="False"
                     IsClosable="True"
                     Closed="GlobalInfoBar_Closed"/>

            <Frame x:Name="RootFrame"
                   Grid.Row="2"
                   Margin="0"/>
        </Grid>

        <!-- RIGHT: Sidebar -->
        <controls:AppSidebar x:Name="Sidebar"
                         Grid.Column="1"
                         NavigateRequested="Sidebar_NavigateRequested"
                         LogoutRequested="Sidebar_LogoutRequested" />
    </Grid>
</Window>
